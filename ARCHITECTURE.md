# J-Flash 일본어 학습 웹앱 - 시스템 아키텍처

> **버전**: 1.2.0
> **최종 수정**: 2026-01-12
> **상태**: Epic 1-8 구현 완료 ✅ (Epic 9 NAS 배포 진행 중)

---

## 1. 전체 개요

### 1.1 프로젝트 목적
교재 사진을 통해 일본어 단어, 문법, 한자를 자동 추출하고 Anki 스타일의 반복 학습을 제공하는 **완전 로컬** 개인용 웹 애플리케이션

### 1.2 핵심 가치
| 가치 | 설명 |
|------|------|
| 🕐 시간 절약 | OCR로 자동 단어 추출, 수동 입력 불필요 |
| 📚 맞춤형 학습 | 본인 교재에 최적화된 단어장 |
| 🧠 과학적 암기 | SM-2 SRS 알고리즘 기반 장기 기억 강화 |
| 🔒 프라이버시 | 모든 데이터 로컬/NAS 저장 |
| 📱 외부 복습 | 복습은 어디서든, OCR은 로컬에서 |

### 1.3 시스템 아키텍처 (하이브리드)

**핵심 원칙**: OCR/단어 추가 → 로컬 전용 | 복습 → 어디서든

```
┌─────────────────────────────────────────────────────────────────┐
│                   🏠 로컬 환경 (집/사무실 PC)                     │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────────┐   │
│  │ 📸 OCR 업로드  │  │ ✏️ 단어 편집   │  │ 📊 통계/관리      │   │
│  │ (EasyOCR)     │  │ (추가/삭제)   │  │ (전체 기능)       │   │
│  └───────┬───────┘  └───────┬───────┘  └─────────┬─────────┘   │
│          └──────────────────┼──────────────────────┘            │
│                             ▼                                    │
│     ┌─────────────────────────────────────────────────────┐    │
│     │  🖥️ Backend FULL (FastAPI + EasyOCR + Fugashi)      │    │
│     │  localhost:8000  [~500MB 의존성]                     │    │
│     └───────────────────────┬─────────────────────────────┘    │
│                             ▼                                    │
│              ┌─────────────────────────────┐                    │
│              │  🗄️ SQLite DB               │                    │
│              │  data/japanese_learning.db  │ ◄── Git 추적      │
│              └─────────────────────────────┘                    │
└─────────────────────────────────────────────────────────────────┘
                             │
                      git push (DB 포함)
                             ▼
                    ┌─────────────────┐
                    │   GitHub Repo   │
                    │   (Private)     │
                    └────────┬────────┘
                             │
                      git pull (자동/수동)
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                  🌐 웹 배포 환경 (NAS/Vercel)                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🐳 Backend LITE (OCR 제외, ~50MB)                       │   │
│  │  ┌───────────────┐     ┌───────────────────────────┐    │   │
│  │  │ /api/review/* │     │ SQLite (Git에서 동기화)    │    │   │
│  │  │ /api/stats/*  │     │ 복습 기록만 쓰기 가능      │    │   │
│  │  │ /api/vocab/*  │     └───────────────────────────┘    │   │
│  │  │ (읽기 전용)   │                                       │   │
│  │  └───────────────┘                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                             ▲                                    │
│              Tailscale / Cloudflare Tunnel / Vercel             │
└─────────────────────────────────────────────────────────────────┘
                             │
              ┌──────────────┴──────────────┐
              ▼                              ▼
       📱 모바일 (외부)                💻 노트북 (외부)
         복습만 가능                     복습만 가능
```

### 1.3.1 배포 모드별 기능 비교
| 기능 | 로컬 (FULL) | 웹 (LITE) |
|------|-------------|-----------|
| 📸 OCR 이미지 처리 | ✅ | ❌ |
| ✏️ 단어/문법 추가 | ✅ | ❌ |
| 🗑️ 단어/문법 삭제 | ✅ | ❌ |
| 📝 복습 카드 학습 | ✅ | ✅ |
| 📊 학습 통계 조회 | ✅ | ✅ |
| 🔄 SRS 진행도 업데이트 | ✅ | ✅ |
| 📥 데이터 내보내기 | ✅ | ✅ |
| 📤 데이터 가져오기 | ✅ | ❌ |

### 1.3.2 Git 기반 DB 동기화
```bash
# 로컬에서 단어 추가 후
git add data/japanese_learning.db
git commit -m "Add vocabulary from [교재명]"
git push

# 웹 서버에서 자동 동기화 (webhook 또는 cron)
git pull origin main
```

**동기화 규칙**:
- DB는 바이너리 → 동시 수정 시 충돌
- 권장: 로컬에서만 단어 추가 → Push → 웹에서 복습만
- 웹 학습 기록을 로컬로 가져오려면 Export/Import 사용

### 1.4 로컬 개발 아키텍처
```
┌─────────────────────────────────────────────────────────────────┐
│              Frontend (Next.js 14 + TypeScript)                  │
│                    📱 반응형 UI (Tailwind CSS)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ /upload      │  │ /review      │  │ /grammar     │          │
│  │ 이미지 업로드 │  │ 플래시카드   │  │ /kanji       │          │
│  │ + OCR 결과   │  │ 학습 UI      │  │ 관리 UI      │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  🎨 Tailwind CSS  |  📦 Zustand (State)  |  🔌 Axios (API)     │
└──────────────────────────────────────────────────────────────────┘
                              ↕ REST API (Port 8000)
┌──────────────────────────────────────────────────────────────────┐
│                    Backend (FastAPI + Python 3.10+)              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ OCR Service  │  │ NLP Service  │  │ SRS Service  │          │
│  │ (EasyOCR)    │  │ (Fugashi)    │  │ (SM-2 Algo)  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  📁 /api  |  📊 /models  |  ⚙️ /services  |  📤 /uploads       │
└──────────────────────────────────────────────────────────────────┘
                              ↕ SQLAlchemy ORM
┌──────────────────────────────────────────────────────────────────┐
│                         데이터 계층                               │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 로컬: SQLite | 프로덕션: PostgreSQL (Railway)              │ │
│  │ ┌────────────┬────────────┬────────────┬────────────────┐ │ │
│  │ │ Vocabulary │ SRS_Review │ Grammar    │ Kanji          │ │ │
│  │ │ (단어)     │ (복습관리)  │ (문법)     │ (한자)         │ │ │
│  │ └────────────┴────────────┴────────────┴────────────────┘ │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────┐  ┌────────────────────┐                 │
│  │ 로컬: uploads/     │  │ 프로덕션:          │
│  │ 프로덕션: S3/R2    │  │ Railway Volumes    │                 │
│  │ (이미지 저장)       │  │ (OCR/학습 기록)     │                 │
│  └────────────────────┘  └────────────────────┘                 │
└──────────────────────────────────────────────────────────────────┘
```

## 2. 기술 스택

### 2.1 Frontend
| 기술 | 버전 | 용도 |
|------|------|------|
| Next.js | 14.x | React 프레임워크 (App Router) |
| TypeScript | 5.x | 정적 타입 |
| Tailwind CSS | 3.x | 유틸리티 기반 스타일링 |
| Zustand | 4.x | 경량 상태 관리 |
| Axios | 1.x | HTTP 클라이언트 |

### 2.2 Backend
| 기술 | 버전 | 용도 |
|------|------|------|
| FastAPI | 0.109+ | 비동기 웹 프레임워크 |
| Uvicorn | 0.27+ | ASGI 서버 |
| EasyOCR | 1.7+ | 일본어 OCR 엔진 |
| Fugashi | 1.3+ | MeCab 형태소 분석 래퍼 |
| SQLAlchemy | 2.0+ | ORM |
| Pillow | 10.x | 이미지 처리 |
| OpenCV | 4.9+ | 이미지 전처리 |

### 2.3 데이터베이스
| 항목 | 설명 |
|------|------|
| DBMS | SQLite 3 |
| 파일 위치 | `data/japanese_learning.db` |
| ORM | SQLAlchemy 2.0+ |
| 테이블 | 7개 (Vocabulary, SRS_Review, Grammar, Kanji, Vocab_Kanji, OCR_Logs, Study_Logs) |

### 2.4 스토리지
| 용도 | 경로 |
|------|------|
| 업로드 이미지 | `backend/uploads/` |
| 데이터베이스 | `data/japanese_learning.db` |
| 로그 파일 | `backend/logs/` (선택) |

## 3. 데이터 흐름 (Data Flow)

### 3.1 이미지 업로드 및 처리 파이프라인
```
1. 사용자 이미지 업로드 (Frontend)
   ↓
2. Multipart FormData로 Backend 전송
   ↓
3. Backend: 이미지 파일 저장 (./uploads/)
   ↓
4. OCR 처리 (EasyOCR) → 일본어 텍스트 추출
   ↓
5. OCR 품질 검증 → 상태/경고 생성 (평균 신뢰도, 일본어 문자 비율, 최소 글자수)
   ↓
6. 형태소 분석 (Fugashi) → 단어/문법 단위 분리
   ↓
7. 후처리: 중복 제거, 의미 있는 단어 필터링
   ↓
8. DB 저장 후 사용자에게 결과 반환
   ↓
9. Frontend: 추출 결과 + 검증 상태/경고 표시, 수동 교정 UI 제공
```

#### 3.1.1 OCR 검증 기준 (MVP)
- 파일 형식/크기 검증 (jpg/png, 10MB 이하)
- 평균 신뢰도(Confidence) 임계치 미만이면 `warn`
- 일본어 문자 비율이 낮으면 `warn` (한자/히라가나/가타카나 비율)
- 최소 글자수 부족 시 `warn` (교재 페이지 미촬영 가능성)
- `fail` 시 재촬영 가이드 제공 + 수동 입력 허용

### 3.2 학습 카드 처리 파이프라인
```
1. 사용자: "오늘의 복습" 요청
   ↓
2. Backend: SRS 알고리즘으로 next_review <= NOW()인 단어 조회
   ↓
3. Frontend: 플래시카드 형태로 표시
   ↓
4. 사용자: "알고 있음"/"모름" 입력
   ↓
5. Backend: SM-2 알고리즘으로 다음 복습일 계산 및 업데이트
   ↓
6. 다음 카드 표시
```

## 4. 디렉토리 구조

### 4.1 현재 프로젝트 구조 (구축 완료 ✅)
```
learning-japanese/
├── 📁 frontend/                 # Next.js 14 애플리케이션 ✅
│   ├── app/                     # App Router
│   │   ├── layout.tsx           # 루트 레이아웃 ✅
│   │   ├── page.tsx             # 메인 페이지 ✅
│   │   ├── globals.css          # 전역 스타일 ✅
│   │   ├── upload/              # 이미지 업로드 + OCR ✅
│   │   ├── extract/             # 단어 추출 UI ✅
│   │   ├── vocab/               # 단어장 관리 ✅
│   │   ├── review/              # 플래시카드 학습 ✅
│   │   ├── grammar/             # 문법 관리 ✅
│   │   ├── kanji/               # 한자 학습 ✅
│   │   ├── stats/               # 통계 대시보드 ✅
│   │   └── data/                # 데이터 내보내기/가져오기 ✅
│   ├── components/              # 재사용 컴포넌트 ✅
│   │   └── FlashCard.tsx        # 플래시카드 UI ✅
│   ├── lib/                     # 유틸리티 ✅
│   │   └── api.ts               # API 클라이언트 ✅
│   ├── public/                  # 정적 파일
│   ├── package.json             # ✅ zustand, axios 설치됨
│   └── tsconfig.json            # TypeScript 설정
│
├── 📁 backend/                  # FastAPI 애플리케이션 ✅
│   ├── main.py                  # ✅ FastAPI 엔트리포인트
│   ├── api/                     # API 엔드포인트 ✅
│   │   ├── upload.py            # 이미지 업로드 API ✅
│   │   ├── ocr.py               # OCR 처리 API ✅
│   │   ├── vocab.py             # 단어 CRUD API ✅
│   │   ├── review.py            # SRS 복습 API ✅
│   │   ├── grammar.py           # 문법 API ✅
│   │   ├── kanji.py             # 한자 API ✅
│   │   ├── stats.py             # 통계 API ✅
│   │   └── export_import.py     # 데이터 내보내기/가져오기 API ✅
│   ├── models/                  # SQLAlchemy 모델 ✅
│   │   ├── vocabulary.py        # 단어 모델 ✅
│   │   ├── grammar.py           # 문법 모델 ✅
│   │   ├── kanji.py             # 한자 모델 ✅
│   │   └── study_log.py         # 학습 로그 모델 ✅
│   ├── services/                # 비즈니스 로직 ✅
│   │   ├── ocr_service.py       # EasyOCR 처리 ✅
│   │   ├── morphology_service.py # Fugashi 형태소 분석 ✅
│   │   ├── kanji_service.py     # 한자 정보 조회 ✅
│   │   └── srs_service.py       # SM-2 알고리즘 ✅
│   ├── uploads/                 # 업로드 이미지 저장
│   ├── Dockerfile               # Docker 이미지 설정 ✅
│   ├── pyproject.toml           # ✅ 의존성 정의
│   └── requirements.txt         # ✅ pip 호환 의존성
│
├── 📁 data/                     # 데이터 저장소 ✅
│   └── japanese_learning.db     # ✅ SQLite DB (7개 테이블)
│
├── 📁 docs/                     # 문서 디렉토리
│
├── 📄 ARCHITECTURE.md           # 이 파일
├── 📄 PRD.md                    # 제품 요구사항
├── 📄 MVP.md                    # MVP 정의
└── 📄 schema.sql                # DB 스키마 정의
```

### 4.2 구현된 컴포넌트 구조 ✅
```
frontend/
├── app/
│   ├── upload/page.tsx          # 드래그앤드롭 이미지 업로드 + OCR ✅
│   ├── extract/page.tsx         # OCR 결과 단어 추출 UI ✅
│   ├── vocab/page.tsx           # 단어장 CRUD 관리 ✅
│   ├── review/page.tsx          # 플래시카드 복습 UI ✅
│   ├── grammar/page.tsx         # 문법 관리 UI ✅
│   ├── kanji/page.tsx           # 한자 학습 UI ✅
│   ├── stats/page.tsx           # 통계 대시보드 ✅
│   └── data/page.tsx            # 데이터 내보내기/가져오기 ✅
├── components/
│   └── FlashCard.tsx            # 플래시카드 컴포넌트 ✅
└── lib/
    └── api.ts                   # Axios API 클라이언트 ✅
```

## 5. API 엔드포인트 설계

### 5.1 기본 엔드포인트 (구현 완료 ✅)
| Method | Endpoint | 설명 | 상태 |
|--------|----------|------|------|
| GET | `/health` | 헬스체크 | ✅ |
| GET | `/` | API 정보 | ✅ |

### 5.2 이미지 업로드 API (구현 완료 ✅)
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/upload/image` | 이미지 업로드 (JPG/PNG, 최대 10MB) |

**Request**: `multipart/form-data`
```
file: <image_file>
```

**Response**:
```json
{
  "success": true,
  "filename": "a1b2c3d4e5f6...32자리UUID.jpg",
  "path": "/uploads/a1b2c3d4e5f6...32자리UUID.jpg"
}
```

### 5.2.1 OCR 처리 API (구현 완료 ✅)
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/ocr/process` | 업로드된 이미지에서 일본어 텍스트 인식 |

**Request**:
```json
{
  "image_path": "/uploads/a1b2c3d4...jpg"
}
```

**Response**:
```json
{
  "success": true,
  "results": [
    {
      "text": "日本語",
      "confidence": 0.95,
      "confidence_level": "high",
      "warning": false,
      "bbox": [[x1,y1], [x2,y2], [x3,y3], [x4,y4]]
    }
  ],
  "full_text": "日本語のテキスト",
  "processing_time_ms": 2500
}
```

**보안**: 경로 조작 방지 (UUID 파일명만 허용, UPLOAD_DIR 외부 접근 차단)

### 5.2.2 형태소 분석 API (구현 완료 ✅)
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/morphology/analyze` | 일본어 텍스트 형태소 분석 |

**제한 사항**:
- `text` 최대 길이: 5,000자 (메모리/성능 최적화)
- 빈 문자열 불가

**Request**:
```json
{
  "text": "日本語を勉強する",
  "filter_particles": true,
  "include_reading_hiragana": true
}
```

**Response**:
```json
{
  "success": true,
  "words": [
    {
      "surface": "日本語",
      "reading": "ニホンゴ",
      "reading_hiragana": "にほんご",
      "pos": "名詞",
      "pos_detail": "一般",
      "base_form": "日本語",
      "is_content_word": true
    }
  ],
  "total_count": 5,
  "filtered_count": 3,
  "processing_time_ms": 15
}
```

### 5.3 단어 관리 API
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/vocab` | 단어 목록 (검색, 페이지네이션) |
| POST | `/api/vocab` | 새 단어 추가 |
| POST | `/api/vocab/bulk` | 단어 일괄 추가 (OCR 결과 저장) |
| GET | `/api/vocab/{id}` | 단일 단어 조회 |
| PUT | `/api/vocab/{id}` | 단어 수정 |
| DELETE | `/api/vocab/{id}` | 단어 삭제 |
| GET | `/api/vocab/{id}/image` | 단어 이미지 조회 (has_image=true인 경우) |
| GET | `/api/vocab/stats/summary` | 단어 통계 (총 개수, 학습 현황) |

**POST /api/vocab/bulk Request**:
```json
{
  "words": [
    {
      "kanji": "日本語",
      "reading": "にほんご",
      "meaning": "일본어",
      "pos": "名詞",
      "source_img": "/uploads/abc123def456.jpg"
    }
  ]
}
```

**이미지 저장 설계**:
- 한 OCR 이미지에서 추출된 여러 단어는 같은 `source_img` 경로를 공유
- 이미지 바이너리는 **첫 번째 단어**에만 저장 (DB 용량 절약)
- `has_image: true`인 단어만 `/api/vocab/{id}/image`로 이미지 조회 가능

**GET /api/vocab/stats/summary Response**:
```json
{
  "total": 150,
  "due_today": 25,
  "learned": 120,
  "new": 30
}
```

### 5.4 SRS 학습 API
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/review/cards` | 오늘의 복습 카드 조회 (limit 파라미터, 기본 20, 최대 100) |
| POST | `/api/review/answer` | 학습 결과 제출 |
| GET | `/api/review/stats` | 복습 통계 조회 |
| POST | `/api/review/reset/{vocab_id}` | 특정 단어의 SRS 진행도 초기화 |

**POST /api/review/answer Request**:
```json
{
  "vocab_id": 1,
  "known": true
}
```

**GET /api/review/stats Response**:
```json
{
  "due_now": 15,
  "due_today": 25,
  "due_this_week": 80,
  "total_reviewed": 150
}
```

**POST /api/review/reset/{vocab_id} Response**:
```json
{
  "message": "SRS progress reset",
  "vocab_id": 1
}
```

### 5.5 문법 API ✅
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/grammar` | 문법 목록 (page, page_size, search, level, sort_by, sort_order) |
| GET | `/api/grammar/levels` | JLPT 레벨 목록 조회 |
| GET | `/api/grammar/stats/summary` | 문법 통계 (전체, 레벨별 개수) |
| GET | `/api/grammar/{id}` | 문법 상세 조회 |
| POST | `/api/grammar` | 문법 추가 |
| PUT | `/api/grammar/{id}` | 문법 수정 |
| DELETE | `/api/grammar/{id}` | 문법 삭제 |

**GET /api/grammar 파라미터**:
- `page`: 페이지 번호 (기본: 1)
- `page_size`: 페이지 크기 (기본: 20, 최대: 100)
- `search`: 검색어 (title, meaning, description, example 검색)
- `level`: JLPT 레벨 필터 (N5, N4, N3, N2, N1)
- `sort_by`: 정렬 기준 (created_at, title, level)
- `sort_order`: 정렬 순서 (asc, desc)

**GET /api/grammar/levels Response**:
```json
["N5", "N4", "N3", "N2", "N1"]
```

**GET /api/grammar/stats/summary Response**:
```json
{
  "total": 50,
  "by_level": {
    "N5": 15,
    "N4": 12,
    "N3": 10,
    "N2": 8,
    "N1": 3,
    "none": 2
  }
}
```

**POST /api/grammar Request**:
```json
{
  "title": "～ている",
  "meaning": "~하고 있다",
  "description": "진행 또는 상태를 나타냄",
  "example": "今、本を読んでいます。",
  "example_meaning": "지금, 책을 읽고 있습니다.",
  "level": "N5"
}
```

### 5.6 통계 대시보드 API ✅
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/stats/overview` | 전체 학습 현황 (단어 수, 진행도) |
| GET | `/api/stats/daily` | 일별 학습 통계 (days 파라미터, 기본 7일) |
| GET | `/api/stats/accuracy` | 정답률 추이 그래프 데이터 (days 파라미터, 기본 14일) |
| GET | `/api/stats/streak` | 학습 연속일 정보 |
| GET | `/api/stats/dashboard` | 대시보드 통합 데이터 (위 4개 한번에 조회) |

**GET /api/stats/overview Response**:
```json
{
  "total_words": 150,
  "learned_words": 120,
  "mastered_words": 45,
  "new_words": 30,
  "due_today": 25,
  "total_grammar": 50,
  "learning_progress": 80.0
}
```

**GET /api/stats/daily Response** (days=7):
```json
[
  {
    "date": "2025-01-06",
    "total_reviews": 50,
    "correct": 42,
    "incorrect": 8,
    "accuracy": 84.0,
    "new_words_learned": 5
  }
]
```

**GET /api/stats/accuracy Response** (days=14):
```json
{
  "dates": ["01/01", "01/02", ...],
  "accuracy": [80.0, 85.0, ...],
  "total_reviews": [50, 45, ...]
}
```

**GET /api/stats/streak Response**:
```json
{
  "current_streak": 7,
  "longest_streak": 14,
  "last_study_date": "2025-01-12"
}
```

### 5.7 한자 API ✅
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/kanji/info/{character}` | 단일 한자 정보 조회 |
| POST | `/api/kanji/analyze` | 텍스트에서 한자 추출 및 분석 |
| GET | `/api/kanji/word/{word}` | 단어의 한자 분석 |
| GET | `/api/kanji/extract` | 텍스트에서 한자 문자만 추출 |
| GET | `/api/kanji/stats` | 한자 통계 (저장된 한자, JLPT별) |
| GET | `/api/kanji/vocab/{vocab_id}/kanji` | 단어장 항목의 한자 분석 |

**GET /api/kanji/info/{character} Response**:
```json
{
  "character": "日",
  "on_readings": ["ニチ", "ジツ"],
  "kun_readings": ["ひ", "か"],
  "meanings": ["day", "sun", "Japan"],
  "meanings_ko": ["날", "해", "일본"],
  "stroke_count": 4,
  "jlpt_level": 5
}
```

**POST /api/kanji/analyze Request/Response**:
```json
// Request
{ "text": "日本語を勉強する" }

// Response
{
  "kanji_count": 5,  // 고유 한자 수 (중복 제외)
  "kanji_list": [...]
}
```

**GET /api/kanji/stats Response**:
```json
{
  "total_saved": 0,
  "by_level": { "N5": 0, "N4": 0, "N3": 0, "N2": 0, "N1": 0 },
  "dictionary_size": 122
}
```

**한자 사전 설계 (kanji_service.py)**:
- 현재: 코드 내장 사전 (KANJI_DICT) - N5 약 100자, N4 일부 포함
- 확장 방안: 외부 JSON 파일 또는 DB 기반 확장 가능
- `extract_kanji_from_text()`: 고유 한자만 반환 (중복 제거, 출현 순서 유지)

### 5.8 Export/Import API ✅

데이터 백업 및 복원을 위한 API. CSV/JSON 형식 지원.

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/data/vocab/csv` | 단어장 CSV 내보내기 |
| GET | `/api/data/vocab/json` | 단어장 JSON 내보내기 |
| GET | `/api/data/grammar/csv` | 문법 CSV 내보내기 |
| GET | `/api/data/grammar/json` | 문법 JSON 내보내기 |
| GET | `/api/data/all/json` | 전체 백업 (단어+문법) JSON |
| POST | `/api/data/vocab/csv` | 단어장 CSV 가져오기 |
| POST | `/api/data/vocab/json` | 단어장 JSON 가져오기 |
| POST | `/api/data/grammar/csv` | 문법 CSV 가져오기 |
| POST | `/api/data/grammar/json` | 문법 JSON 가져오기 |
| POST | `/api/data/all/json` | 전체 백업 JSON 가져오기 |
| GET | `/api/data/stats` | 내보내기 통계 (항목 수 미리보기) |

**중복 판단 로직**:
- **Vocabulary**: `(kanji, reading, meaning)` 복합 키
  - 같은 kanji라도 reading/meaning이 다르면 별개 항목으로 취급
  - 예: 生 (なま/raw) vs 生 (せい/life) 는 별개 항목
- **Grammar**: `(title, meaning)` 복합 키
  - 같은 문법 제목이라도 의미가 다르면 별개 항목

**Import 파라미터**:
- `skip_duplicates`: 중복 건너뛰기 (기본: true)

**파일 검증**:
- 확장자 검증: `.csv` 또는 `.json`
- MIME 타입 검증: `text/csv`, `application/json` 등

**GET /api/data/stats Response**:
```json
{
  "vocabulary_count": 150,
  "grammar_count": 50,
  "exported_at": "2025-01-12T10:30:00"
}
```

**내보내기 CSV 형식 (Vocabulary)**:
```csv
kanji,reading,meaning,pos,source_img,reps,interval,ease_factor,next_review,created_at
日本語,にほんご,일본어,名詞,,3,6,2.5,2025-01-18T00:00:00,2025-01-01T10:00:00
```

**내보내기 JSON 형식 (Vocabulary)**:
```json
{
  "version": "1.0",
  "type": "vocabulary",
  "exported_at": "2025-01-12T10:30:00",
  "count": 150,
  "items": [
    {
      "kanji": "日本語",
      "reading": "にほんご",
      "meaning": "일본어",
      "pos": "名詞",
      "source_img": null,
      "reps": 3,
      "interval": 6,
      "ease_factor": 2.5,
      "next_review": "2025-01-18T00:00:00",
      "created_at": "2025-01-01T10:00:00"
    }
  ]
}
```

**POST Import Response**:
```json
{
  "success": true,
  "vocabulary_imported": 45,
  "vocabulary_skipped": 5,
  "grammar_imported": 0,
  "grammar_skipped": 0,
  "errors": ["Row 12: kanji 필드가 비어있습니다."]
}
```

**성능 고려사항**:
- 현재: 전체 파일 메모리 로드 (소규모 학습용 데이터 가정)
- 권장 최대: 10MB (약 50,000개 항목)
- 대용량 처리가 필요한 경우: 스트리밍 파서 또는 청크 처리 권장

## 6. 핵심 알고리즘

### 6.1 SRS (Spaced Repetition System) - SM-2 변형
- **초기값**: interval=0, ease_factor=2.5, reps=0
- **성공 시**:
  - reps=0: interval=1일
  - reps=1: interval=6일
  - reps≥2: interval = interval × ease_factor (최대 365일)
  - ease_factor += 0.1 (최대 2.5)
  - reps += 1
- **실패 시**:
  - interval = 1일
  - ease_factor -= 0.2 (최소 1.3)
  - **reps는 유지** (설계 의도: 학습 이력 보존)
- **다음 복습일**: next_review = today + interval

**설계 의도 (reps 미리셋)**:
- reps는 "해당 단어를 맞춘 총 횟수"를 추적
- 오답 시에도 학습 이력을 보존하여 진행도 표시에 활용
- 표준 SM-2는 reps를 리셋하지만, 이 앱에서는 사용자 동기 부여를 위해 학습 이력 보존을 우선함

### 6.2 형태소 분석 후처리
1. 불필요한 품사 제외 (조사, 어미 등)
2. 중복 단어 제거
3. 한자 포함 여부 확인 (한자가 있는 단어 우선)
4. 빈도수 기반 중요 단어 추출 (선택사항)

### 6.3 OCR 정확도 향상
1. 이미지 전처리 (그레이스케일, 명암 조정)
2. 후리가나 자동 인식
3. 사용자 수동 교정 UI 제공

## 7. 보안 및 개인정보
- 모든 데이터는 로컬에서만 처리 (클라우드 업로드 없음)
- SQLite DB 파일 암호화 (선택사항)
- 이미지 파일 저장 경로 제한

## 8. 확장 가능성
- 나중에 클라우드 연동 시: AWS S3, Supabase 등
- 음성 합성(TTS) 추가
- 통계 대시보드 (학습 진도, 정답률 그래프)
- 모바일 앱 연동 (React Native)

## 9. 개발 환경 설정

### 9.1 필수 요구사항
| 항목 | 버전 |
|------|------|
| Python | 3.10+ |
| Node.js | 18+ |
| npm | 9+ |

### 9.2 Backend 환경 설정
```bash
cd backend

# 가상환경 생성 및 활성화
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt

# 서버 실행
uvicorn main:app --reload --port 8000

# 확인: http://localhost:8000/health
```

### 9.3 Frontend 환경 설정
```bash
cd frontend

# 의존성 설치 (이미 완료됨)
npm install

# 개발 서버 실행
npm run dev

# 확인: http://localhost:3000
```

### 9.4 MeCab 설치 (형태소 분석용)
```bash
# macOS (Homebrew)
brew install mecab mecab-ipadic

# Ubuntu/Debian
sudo apt-get install mecab libmecab-dev mecab-ipadic-utf8

# Python 래퍼 (권장 - requirements.txt에 포함됨)
pip install fugashi unidic-lite
```

### 9.5 데이터베이스 초기화
```bash
# schema.sql로 DB 생성 (이미 완료됨)
sqlite3 data/japanese_learning.db < schema.sql

# 테이블 확인
sqlite3 data/japanese_learning.db ".tables"
# 출력: Grammar  Kanji  OCR_Logs  SRS_Review  Study_Logs  Vocab_Kanji  Vocabulary
```

### 9.6 📱 모바일 접속 설정

같은 Wi-Fi 네트워크에서 핸드폰으로 접속하려면:

#### Step 1: PC의 로컬 IP 확인
```bash
# macOS
ipconfig getifaddr en0

# 또는
ifconfig | grep "inet " | grep -v 127.0.0.1
# 예: 192.168.0.10
```

#### Step 2: 서버를 외부 접속 가능하게 실행

**Backend (FastAPI)**:
```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
# 모바일에서: http://192.168.0.10:8000
```

**Frontend (Next.js)**:
```bash
cd frontend
npm run dev -- -H 0.0.0.0
# 모바일에서: http://192.168.0.10:3000
```

#### Step 3: Frontend 환경 변수 설정
```bash
# frontend/.env.local 파일 생성
NEXT_PUBLIC_API_URL=http://192.168.0.10:8000
```

#### 📱 모바일 접속 URL
| 서비스 | URL |
|--------|-----|
| Frontend | `http://<PC_IP>:3000` |
| Backend API | `http://<PC_IP>:8000` |
| API 문서 | `http://<PC_IP>:8000/docs` |

> ⚠️ **참고**: 같은 Wi-Fi 네트워크에 연결되어 있어야 합니다.

### 9.7 🐳 NAS Docker 배포 (복습 전용)

로컬에서 완벽하게 개발한 후, NAS에 Docker로 배포합니다.
**복습 기능만** 외부에서 접속 가능하고, OCR/편집은 로컬에서 수행합니다.

#### 배포 전략
| 기능 | 실행 환경 | 접속 방식 |
|------|----------|----------|
| 📸 OCR 업로드 | 로컬 PC | localhost:3000 |
| ✏️ 단어 편집 | 로컬 PC | localhost:3000 |
| 📊 통계/관리 | 로컬 PC | localhost:3000 |
| 🎴 **복습 (플래시카드)** | **NAS Docker** | **Tailscale/Tunnel** |

#### 비용: **$0 (완전 무료)**

---

#### 🏠 Part 1: 로컬 개발 완료 후 진행

```bash
# 로컬에서 모든 기능이 완벽히 작동하는지 확인
cd backend && uvicorn main:app --reload --port 8000
cd frontend && npm run dev

# 테스트: http://localhost:3000
```

---

#### 🐳 Part 2: Docker 설정

##### Dockerfile (backend/)
```dockerfile
FROM python:3.11-slim

WORKDIR /app

# 시스템 의존성 (OCR 제외 - 복습만 지원)
RUN apt-get update && apt-get install -y \
    libsqlite3-dev \
    && rm -rf /var/lib/apt/lists/*

# Python 의존성
COPY requirements-docker.txt .
RUN pip install --no-cache-dir -r requirements-docker.txt

# 앱 코드 복사
COPY . .

# 포트 노출
EXPOSE 8000

# 실행
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

##### requirements-docker.txt (경량 버전, OCR 제외)
```
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.25
pydantic>=2.5.3
```

##### docker-compose.yml (루트)
```yaml
version: '3.8'

services:
  jflash-review:
    build: ./backend
    container_name: jflash-review
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data:ro  # 읽기 전용 DB 마운트
    environment:
      - ENVIRONMENT=production
      - REVIEW_ONLY=true
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # 선택: 프론트엔드도 Docker로 배포 시
  jflash-frontend:
    build: ./frontend
    container_name: jflash-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://jflash-review:8000
    depends_on:
      - jflash-review
    restart: unless-stopped
```

---

#### 📤 Part 3: NAS 배포

##### Step 1: DB 동기화 스크립트
```bash
#!/bin/bash
# sync-db-to-nas.sh

NAS_PATH="/volume1/docker/jflash/data"
LOCAL_DB="./data/japanese_learning.db"

# DB 복사 (로컬 → NAS)
scp $LOCAL_DB nas:$NAS_PATH/

echo "✅ DB 동기화 완료: $(date)"
```

##### Step 2: Synology NAS에 Docker 배포
```bash
# NAS SSH 접속
ssh nas

# 프로젝트 클론 또는 복사
cd /volume1/docker
git clone <your-repo> jflash
# 또는: scp -r ./learning-japanese nas:/volume1/docker/jflash

# Docker 빌드 및 실행
cd jflash
docker-compose up -d --build

# 상태 확인
docker-compose ps
docker-compose logs -f
```

##### Step 3: Synology Docker GUI (대안)
1. Container Manager 열기
2. 프로젝트 → 생성 → docker-compose.yml 업로드
3. 빌드 및 실행

---

#### 🌐 Part 4: 외부 접속 설정

##### 옵션 A: Tailscale (권장, 가장 쉬움)
```bash
# NAS에 Tailscale 설치
# Synology 패키지 센터 → Tailscale 설치

# 접속 URL
# http://nas.tailnet-name.ts.net:3000
```

##### 옵션 B: Cloudflare Tunnel (무료, 도메인 필요)
```bash
# NAS에 cloudflared 설치
docker run -d --name cloudflared \
  cloudflare/cloudflared:latest tunnel \
  --no-autoupdate run --token <YOUR_TUNNEL_TOKEN>

# Cloudflare Dashboard에서 터널 설정
# jflash.yourdomain.com → localhost:3000
```

##### 옵션 C: Synology QuickConnect + 역방향 프록시
1. 제어판 → 외부 액세스 → QuickConnect 활성화
2. 제어판 → 로그인 포털 → 고급 → 역방향 프록시 설정

---

#### 🔄 Part 5: DB 동기화 워크플로우

```
1. 📸 로컬에서 OCR 및 단어 추가
   ↓
2. 📝 단어 편집 및 저장 (로컬 SQLite)
   ↓
3. 🔄 DB 동기화 (수동 또는 자동)
   $ ./sync-db-to-nas.sh
   ↓
4. 📱 외부에서 복습 (NAS 접속)
```

##### 자동 동기화 (선택)
```bash
# crontab -e
# 매 시간 DB 동기화
0 * * * * /path/to/sync-db-to-nas.sh >> /var/log/jflash-sync.log 2>&1
```

---

#### 📊 비용 비교
| 항목 | 클라우드 (Railway) | NAS Docker |
|------|-------------------|------------|
| 월 비용 | $5 | **$0** |
| 외부 접속 | ✅ 항상 | ✅ NAS 켜져있을 때 |
| OCR 기능 | ✅ 가능 | ❌ 로컬만 |
| 설정 난이도 | 쉬움 | 중간 |
| 데이터 위치 | 클라우드 | **내 NAS** |

---

#### ✅ 배포 체크리스트
- [ ] 로컬에서 모든 기능 테스트 완료
- [ ] Dockerfile 작성
- [ ] docker-compose.yml 작성
- [ ] NAS에 Docker 환경 준비
- [ ] DB 동기화 스크립트 작성
- [ ] 외부 접속 설정 (Tailscale/Tunnel)
- [ ] 모바일에서 복습 테스트

---

## 10. 개발 로드맵

### Phase 1: MVP (v1.0) ✅ 완료
| Epic | 목표 | 상태 |
|------|------|------|
| Epic 1 | 환경 구축 (프로젝트 구조, DB 초기화) | ✅ 완료 |
| Epic 2 | 이미지 업로드 + OCR 처리 | ✅ 완료 |
| Epic 3 | 단어장 관리 (CRUD) | ✅ 완료 |
| Epic 4 | 플래시카드 복습 + SM-2 알고리즘 | ✅ 완료 |

### Phase 2: v1.1 ✅ 완료
| Epic | 목표 | 상태 |
|------|------|------|
| Epic 5 | 문법 관리 기능 | ✅ 완료 |
| Epic 6 | 한자 학습 (음독/훈독) | ✅ 완료 |

### Phase 3: v1.2 ✅ 완료
| Epic | 목표 | 상태 |
|------|------|------|
| Epic 7 | 통계 대시보드 | ✅ 완료 |
| Epic 8 | 데이터 내보내기/가져오기 | ✅ 완료 |
| Epic 9 | NAS Docker 배포 | ✅ 완료 |

---

## 11. 참고 자료

| 자료 | 링크 |
|------|------|
| EasyOCR | https://github.com/JaidedAI/EasyOCR |
| Fugashi | https://github.com/polm/fugashi |
| SM-2 알고리즘 | https://www.supermemo.com/en/archives1990-2015/english/ol/sm2 |
| FastAPI | https://fastapi.tiangolo.com/ |
| Next.js | https://nextjs.org/docs |
| Tailwind CSS | https://tailwindcss.com/docs |
| Zustand | https://github.com/pmndrs/zustand |
| Vercel | https://vercel.com/docs |
| Railway | https://docs.railway.app/ |
| Cloudflare R2 | https://developers.cloudflare.com/r2/ |

---

## 12. 변경 이력

| 날짜 | 버전 | 변경 내용 |
|------|------|----------|
| 2025-01-11 | 1.0.0 | 초기 환경 구축 완료, 문서 구조화 |
| 2025-01-11 | 1.0.1 | Vercel + Railway 클라우드 배포 아키텍처 추가 |
| 2025-01-11 | 1.0.2 | 하이브리드 배포로 변경 (로컬 OCR + NAS 복습) |
| 2026-01-12 | 1.1.0 | Epic 1-6 완료 (MVP + 문법/한자 관리) |
| 2026-01-12 | 1.2.0 | Epic 7-9 완료 (통계 대시보드, 데이터 내보내기/가져오기, NAS Docker 배포) |
